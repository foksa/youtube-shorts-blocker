Download **youtube-shorts-blocker.zip** under Assets below. Extract it into a permanent folder, open `chrome://extensions`, enable **Developer mode**, and use **Load unpacked** to select the extracted folder containing `manifest.json`.

No Git or build tools are needed. The ZIP includes `INSTALL.txt` with installation and update instructions.
